{% from 'icon.njk' import icon %}

{% macro link(
	text = 'Link',
	href = '',
	mode = '',
	size = '',
	iconName = 'info-signs/link',
	external = false,
	target = '',
	rel = '',
	extraClass = ''
) %}
	{% set classes = [
		'link',
		mode and 'link_mode_' + mode or '',
		size and 'link_size_' + size or '',
		extraClass
	] | join(' ') | trim %}

	<a
		href="{{ href | localePath(page.url) }}"
		class="{{ classes }}"
		{% if target %}target="{{ target }}"{% endif %}
		{% if rel %}rel="{{ rel }}"{% endif %}
	>
		{% if iconName %}
			{{ icon(iconName, 'link__icon') }}
		{% endif %}

		<span class="link__text">{{ text }}</span>

		{% if external %}
			{{ icon('simple-arrows/arrow_right', 'link__external') }}
		{% endif %}
	</a>
{% endmacro %}
